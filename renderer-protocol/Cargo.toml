[package]
name = "phi-recorder-protocol"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
license = "GPL-3.0-or-later"

[lib]
name = "phi_recorder_protocol"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.138"
